import {TextField} from "@mui/material";

export default function Settings() {
    return (
        <TextField
            sx={{ maxWidth: 300 }}
            label="API Key"
        >
        </TextField>
    )
}